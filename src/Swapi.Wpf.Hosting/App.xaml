<Application x:Class="Swapi.Wpf.Hosting.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Swapi.Wpf.Hosting">
    <!--
        Application level resources. The startup logic lives in Program.cs and
        App.xaml.cs; this file only declares shared resources and the theme.
    -->
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Start-up appearance; ThemeManager replaces these dictionaries at runtime. -->
                <ResourceDictionary Source="pack://application:,,,/Themes/Light.xaml" />
                <ResourceDictionary Source="pack://application:,,,/Styles/Classic.xaml" />
                <ResourceDictionary Source="pack://application:,,,/Metrics/Comfortable.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Used by the main window to show the progress bar while loading. -->
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

            <!--
                Implicit styles so that the standard controls follow the theme
                colours, the accent style and the metrics. All values are
                referenced with DynamicResource, which is what makes switching
                them at runtime possible.
            -->
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
                <Setter Property="FontSize" Value="{DynamicResource BodyFontSize}" />
            </Style>

            <Style TargetType="TextBox">
                <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
                <Setter Property="CaretBrush" Value="{DynamicResource ForegroundBrush}" />
                <Setter Property="FontSize" Value="{DynamicResource BodyFontSize}" />
                <Setter Property="Padding" Value="{DynamicResource ControlPadding}" />
            </Style>

            <Style TargetType="Button">
                <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource AccentForegroundBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                <Setter Property="FontSize" Value="{DynamicResource BodyFontSize}" />
                <Setter Property="Padding" Value="{DynamicResource ControlPadding}" />
            </Style>

            <Style TargetType="ListBox">
                <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
                <Setter Property="BorderThickness" Value="0" />
            </Style>

            <Style TargetType="ProgressBar">
                <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
